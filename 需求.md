# 项目需求

## 基本信息

| 项 | 内容 |
|----|------|
| 名称 | SoftwareCopyright-CodeDocGen |
| 简称 | SC-CodeDocGen |
| Slogan | Turn your source code into standard copyright code documents in 1 second. |
| 目标用户 | 软件著作权申请者 |

---

## 技术栈

| 层 | 技术 | 说明 |
|---|------|------|
| GUI | **Flet** | Flutter风格 Material Design UI，Python驱动，跨平台 |
| Word生成 | **python-docx** | 原生支持页眉/页脚/页码/分页符/字体 |
| 打包 | **PyInstaller** | 单exe发布，用户无需Python环境 |
| 语言 | **Python 3.10+** | |

核心依赖：`flet`, `python-docx`。其余全部用标准库（`pathlib`, `re`, `json`, `fnmatch`, `asyncio`）。

---

## 项目结构

```
SC-CodeDocGen/
├── main.py                       # 入口
├── ui/
│   ├── main_view.py              # 主界面整体布局
│   ├── config_panel.py           # 右侧配置面板
│   └── widgets/
│       ├── folder_drop_zone.py   # 文件夹选择（点击 + 拖拽）
│       ├── chip_input.py         # 标签输入组件（后缀/屏蔽目录共用）
│       └── code_stats_card.py    # 代码统计结果卡片
├── core/
│   ├── scanner.py                # 文件扫描（遍历 + 后缀过滤 + 目录屏蔽）
│   ├── counter.py                # 代码行统计（去空行 + 去注释）
│   ├── comment_stripper.py       # 多语言注释去除引擎
│   └── docx_generator.py         # Docx生成（页眉 + 页码 + 正文分页）
├── models/
│   ├── config.py                 # 配置数据类（AppConfig）
│   └── presets.py                # 预设数据（后缀、屏蔽目录、页码格式）
├── config.json                   # 用户配置持久化
└── requirements.txt
```

---

## 流程

### 1. 输入

- 用户选择或拖拽一个软件源码文件夹
- 界面展示已选文件夹路径

### 2. 配置

#### 2.1 页眉
- 输入框，填入软件名称 + 版本号，例如：`SC-CodeDocGenV1.0`
- 必填项，为空时阻止生成

#### 2.2 页码格式
- 下拉框预设 + 自定义输入，预设格式如下：

| key | 显示效果 |
|-----|---------|
| `arabic` | 1, 2, 3... |
| `dash` | - 1 -, - 2 -, - 3 -... |
| `emdash` | —— 1 ——, —— 2 ——, —— 3 —— |
| `roman` | Ⅰ, Ⅱ, Ⅲ... |
| `page_cn` | 第 1 页 |
| `page_total` | 第 1 页 共 X 页 |
| `slash` | 1 / X |
| `cn_num` | 第一页 |
| `cn_total` | 第一页 共 X 页 |
| `cn_comma` | 1，2，3，... |
| `custom` | 自定义（用户填入模板，如 `{page}/{total}`） |

#### 2.3 屏蔽文件夹
- 预设列表（Checkbox）：
  `node_modules`, `dist`, `.git`, `__pycache__`, `build`, `target`, `.venv`, `venv`, `.idea`, `.vscode`
- 支持用户输入自定义屏蔽文件夹名称
- 遍历源码目录时跳过这些文件夹

#### 2.4 代码后缀
- 预设列表（Checkbox）：

| .py | .js | .ts | .java | .cpp | .c | .h | .hpp |
| .cs | .go | .rs | .swift | .kt | .rb | .php | .vue |
| .html | .css | .scss | .sql | .sh | .m |

- 支持用户输入自定义后缀（如 `.dart`, `.r` 等）
- 仅匹配这些后缀的文件被纳入代码文档

#### 2.5 代码行数统计
- 独立按钮「统计代码行数」
- 统计逻辑：
  1. 遍历源码目录（排除屏蔽文件夹）
  2. 仅统计匹配后缀的文件
  3. 去除空行（仅含空白的行）
  4. 去除注释行（单行注释 + 多行注释块）
- 统计结果展示格式：`有效代码共 2345 行 ≈ 47 页（50行/页）`

#### 2.6 输出页数模式
- 根据统计结果默认推荐，用户可手动切换：
  - **全部输出**：代码行数不超过 60 页（即 ≤ 3000 行 ）时自动选择
  - **前后各30页**：代码行数超过 60 页（即 > 3000 行）时自动选择，取前1500行 + 后1500行

#### 2.7 每页行数
- 输入框，默认 `50`
- 影响分页和统计计算

### 3. 处理逻辑

```
用户点击「开始生成」
  → 校验（页眉不为空、文件夹已选、后缀不为空）
  → 异步扫描文件（按后缀过滤 + 屏蔽目录过滤）
  → 逐文件读取内容
  → 去除注释（单行 + 多行）
  → 去除空行
  → 根据输出模式截取代码行
  → 生成 docx：
      - 设置页眉（居中，8pt）
      - 设置页脚页码（居中，按选定格式）
      - 正文每 N 行插入分页符
      - 字体 8pt，宋体/微软雅黑
  → 弹窗提示完成，支持直接打开文档
```

#### 3.1 注释去除规则

| 语言 | 单行注释 | 多行注释 |
|------|---------|---------|
| `.py` | `#` | `"""..."""`, `'''...'''` |
| `.js`, `.ts`, `.java`, `.cpp`, `.c`, `.cs`, `.go`, `.rs`, `.swift`, `.kt`, `.m`, `.dart` | `//` | `/*...*/` |
| `.html`, `.vue` | — | `<!--...-->` |
| `.rb`, `.sh` | `#` | — |
| `.sql` | `--` | `/*...*/` |
| `.css`, `.scss` | — | `/*...*/` |
| `.php` | `//`, `#` | `/*...*/` |

> 注释去除为可选功能，默认开启。

### 4. 输出

- 生成标准 `.docx` 文件
- 文件命名：`软著代码文档_{时间戳}.docx`
- 保存位置：默认用户桌面，支持自定义路径
- 生成后弹窗提示，可「打开文档」或「打开文件夹」

### 5. 配置持久化

- 用户上次配置（页眉、页码格式、后缀选择、屏蔽目录、每页行数）自动保存到 `config.json`
- 下次启动时自动加载

---

## 数据模型

```python
@dataclass
class AppConfig:
    header: str = ""                      # 页眉
    page_format: str = "arabic"           # 页码格式key
    custom_page_format: str = ""          # 自定义页码模板
    lines_per_page: int = 50              # 每页行数
    suffixes: list[str] = None            # 选中的代码后缀
    preset_suffix_states: dict = None     # 预设后缀勾选状态
    ignore_dirs: list[str] = None         # 屏蔽目录
    preset_ignore_states: dict = None     # 预设屏蔽目录勾选状态
    output_mode: str = "auto"             # "all" | "split" | "auto"
    output_path: str = ""                 # 输出路径
```

---

